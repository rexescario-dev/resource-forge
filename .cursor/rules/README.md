# Cursor rules

Project rules for Cursor agents.

| Path | Source |
| --- | --- |
| [`workflow.mdc`](workflow.mdc) | Installed from workflow package `cursor` (managed — see repo-root `workflow.yaml`) |

Do not handcraft a parallel engineering-workflow rule; Update/Replace may refresh `workflow.mdc` from the catalog.
