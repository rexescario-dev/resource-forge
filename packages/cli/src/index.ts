/**
 * @resource-forge/cli
 *
 * CLI foundation (RFC-036), Resource validation (RFC-037), package
 * environment doctor (RFC-038), generate resource (RFC-039), init project
 * marker (RFC-040), and generate from-prisma bootstrap (RFC-041). Sole
 * public export: `run`.
 */
export { run } from './run.js';
