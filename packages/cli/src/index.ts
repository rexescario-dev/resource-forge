/**
 * @resource-forge/cli
 *
 * CLI foundation (RFC-036), Resource validation (RFC-037), package
 * environment doctor (RFC-038), and generate resource (RFC-039).
 * Sole public export: `run`.
 */
export { run } from './run.js';
